package views;
import javax.swing.*;
import java.awt.*;

public class MainButton extends JButton {

    public MainButton(String text) {

        super(text);

        setBackground(null);
        setForeground(Color.BLACK);

    }

}
